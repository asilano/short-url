<h1>Editing Shortened Url <%= URI.join(request.base_url, @shortened_url.short_form) %></h1>

<%= render 'form', shortened_url: @shortened_url %>

<%= link_to 'Show', @shortened_url %> |
<%= link_to 'Back', shortened_urls_path %>
